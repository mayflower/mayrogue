// vim: set softtabstop=4

define(['underscore', 'util', 'geometry/rectangle'],
    function(_, Util, Rectangle)
{
    "use strict";

    var Geometry = {
        Rectangle: Rectangle
    };

    return Geometry;
});
