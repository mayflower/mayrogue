// vim:softtabstop=4:shiftwidth=4

/**
 * Change base class. Nothing to see here yet.
 */

define(['underscore', 'util'],
    function(_, Util)
{
    "use strict";

    var Base = Util.extend(Util.Base, {

    });

    return Base;

});
